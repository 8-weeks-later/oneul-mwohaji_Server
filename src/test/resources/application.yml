spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/todaytest?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: ghddydwns0928!!

  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.spatial.dialect.mysql.MySQL8SpatialDialect
    defer-datasource-initialization: true

  sql:
    init:
      mode: always


jwt:
  secret: testtesttesttesttesttesttesttesttesttest
  accessToken:
    expiration-period: 100000
  refreshToken:
    expiration-period: 1000000